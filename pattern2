#include<iostream>
using namespace std;

int main(int args,char** argv)
{
    int nst=1;
    int n;
    cin>>n;
    for(int r=1;r<=n;r++)
    {
       for(int cst=1;cst<=nst;cst++)
        {  
        cout<<"*";
        }nst++;
        cout<<endl;
        }
return 0;  
}
